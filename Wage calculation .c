#include <stdio.h>
int main(){
	int t,s,i,m;
	
	scanf("%d",&t);
	int a0=3500;
	int a1=a0+1500*0.97;
	int a2=a1+(4500-1500)*0.9;
	int a3=a2+(9000-4500)*0.8;
	int a4=a3+(35000-9000)*0.75;
	int a5=a4+(55000-35000)*0.7;
	int a6=a5+(80000-55000)*0.65;

	if(t<=a0)
	s=t;
	if(a0<t&&t<=a1)
	s=3500+(t-1500)/0.97;
	if(a1<t&&t<=a2)
	s=5000+(t-a1)/0.9;
	if(a2<t&&t<=a3)
	s=8000+(t-a2)/0.8;
	if(a3<t&&t<=a4)
	s=12500+(t-a3)/0.75;
	if(a4<t&&t<=a5)
	s=38500+(t-a4)/0.7;
	if(a5<t&&t<=a6)
	s=58500+(t-a5)/0.65;
	if(t>a6)
	s=83500+(t-a6)/0.55;
	for(i=1;i<100000;i++)
	if(-49<(s-100*i)&&(s-100*i)<49)
	s=100*i;

	printf("%d",s);
	return 0;
	
	
	
	
	
	
	
}

